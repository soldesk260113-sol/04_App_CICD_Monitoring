---
# ═══════════════════════════════════════════════════════════════════════════
# Site Playbook: 04_App_CICD_Monitoring Orchestrator
# ═══════════════════════════════════════════════════════════════════════════

# 1. Base CICD (Jenkins/Gitea)
- import_playbook: 05_deploy_cicd.yml

# 2. Configure Jenkins SSH (Depends on Jenkins being up)
- import_playbook: 05_configure_jenkins_ssh.yml

# 3. Docker Registry (Harbor)
- import_playbook: 06_deploy_registry.yml

# 4. Monitoring (Prometheus/Grafana)
- import_playbook: 03_deploy_monitoring.yml

# 5. ArgoCD (GitOps)
- import_playbook: 07_deploy_argocd.yml

# 6. ArgoCD Apps (Initial App Deployment)
- import_playbook: 07_deploy_argocd_apps.yml
