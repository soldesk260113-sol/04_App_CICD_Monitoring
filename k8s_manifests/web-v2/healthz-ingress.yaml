apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cafekec-healthz-ingress
  namespace: production
spec:
  ingressClassName: nginx
  rules:
  - host: cafekec.shop
    http:
      paths:
      - path: /healthz/global-status
        pathType: Exact
        backend:
          service:
            name: energy-api
            port:
              number: 8000

